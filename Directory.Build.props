<Project>

  <!-- Broiler NuGet packaging metadata (vendored; do not edit by hand). -->
  <Import Project="$(MSBuildThisFileDirectory)eng\Broiler.Packaging.props" />

  <!-- Repository-owned packaging overrides. -->
  <PropertyGroup>
    <PackageProjectUrl>https://github.com/Broiler-Platform/Broiler.CSS</PackageProjectUrl>
    <PackageTags>broiler;css;selectors;cascade;parser</PackageTags>
  </PropertyGroup>

</Project>
